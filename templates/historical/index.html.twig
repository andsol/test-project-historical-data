{% extends '/base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-sm-8">
            {% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}